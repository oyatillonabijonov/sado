/**
 * Panelda rasm haqidagi minimal ma'lumot.
 *
 * Direktivasiz modul: bu tipni ham server yuklovchilari (`doc.ts`), ham client
 * formalari (`ImageDrop`, `BlockEditor`) import qiladi.
 */
export type MediaOption = { id: number; label: string; url: string };
